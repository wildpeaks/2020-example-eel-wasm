# Option 1: Run directly in Node

Install **Node 14** from https://nodejs.org/en/ if you don't have it already
(it also installs `npm`).

Install dependencies:

	npm install

Run the example:

	node main.js
